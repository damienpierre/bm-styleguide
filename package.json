{
  "name": "bm-styleguide",
  "version": "1.0.0",
  "description": "",
  "skpm": {
    "main": "bm-styleguide.sketchplugin",
    "manifest": "src/manifest.json"
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "render": "skpm-build --watch --run",
    "render:once": "skpm-build --run",
    "postinstall": "npm run build && skpm-link"
  },
  "author": "Damien Pierre <damien.pierre@bettermarks.com>",
  "license": "MIT",
  "devDependencies": {
    "@skpm/builder": "^0.8.0"
  },
  "dependencies": {
    "chroma-js": "^1.2.2",
    "prop-types": "^15.5.8",
    "react": "^16.3.2",
    "react-sketchapp": "^2.0.0",
    "react-test-renderer": "^16.3.2"
  }
}
